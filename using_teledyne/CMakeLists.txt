cmake_minimum_required(VERSION 2.8 FATAL_ERROR)
project(CUIC)
if ( CMAKE_COMPILER_IS_GNUCC )
  set(CMAKE_CXX_FLAGS  "${CMAKE_CXX_FLAGS} -w")
endif()
add_subdirectory(plog)
add_subdirectory(common)
add_subdirectory(capture_demo)
